#!/usr/bin/env bash
# bin/detect <build-dir>

set -euo pipefail

BUILD_DIR="$1"

if [ -d "$BUILD_DIR/web" ] && [ -d "$BUILD_DIR/api" ]; then
  echo "MAKE WEB ROOT AND MOVE API TO SUBDIRECTORY"
  exit 0
else
  echo "MAKE WEB ROOT NOT DETECTED"
  exit 1
fi
