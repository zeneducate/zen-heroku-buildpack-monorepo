#!/usr/bin/env bash
# bin/detect <build-dir>

set -euo pipefail

BUILD_DIR="$1"

if [ -d "$BUILD_DIR/web" ] && [ -d "$BUILD_DIR/api" ]; then
  echo "Detected Zen Monorepo!"
  exit 0
else
  echo "Not a Zen Monorepo!"
  exit 1
fi
